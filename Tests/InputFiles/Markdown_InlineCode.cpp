/// `some inline`
/// another`inline`code, some`more inline` code, and `more`code
/// Empty `` inline code
/// `Code1``Code2`
/// `Not **bold** or _italic_`
/**
` @brief not @p formatted in @ref any *way.*`
*/
/// Some ``Code with `some other ticks`` and `here` is ```more such `` text```!
/// Another`` ``thing to format.
/// Nothing to ` format here.
/// Nothing to ``` format here.
