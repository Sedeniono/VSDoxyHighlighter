/**
the `backtics @b should win` over bold since they come first
the **bold `should win` over** the backtics since the stars come first
@mainpage Some `inline`, **bold** and *italic* text should loose to titles
@par Some other @b cmd should @ref loose to @a title
*/
